/**
 * 此JS用于总结js常用的一些原生方法
 */

stringArray.join("") //js用于把数组中的所有元素放入一个字符串
stringObject.split(separator,howmany) //用于把一个字符串分割成字符串数组
stringObject.charAt(index) //可返回指定位置的字符
stringObject.toUpperCase() //方法用于把字符串转换为大写
stringObject.substr(start,length) //可在字符串中抽取从 start 下标开始的指定数目的字符
arrayObject.reverse() //用于颠倒数组中元素的顺序
arrayObject.concat(arrayX,arrayX,......,arrayX) //

RegExpObject.test(string) //用于检测一个字符串是否匹配某个模式(如果字符串 string 中含有与 RegExpObject 匹配的文本，则返回 true，否则返回 false。)
