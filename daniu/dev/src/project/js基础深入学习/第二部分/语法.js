/* ============================================================
    此JS用于总结语法的一些概念和代码解释（*代表重要的程度）
============================================================ */
//1:规范定义var的返回值是underfined，所以我们在控制台上打印出var a=...都会返回一个underfined
//2:JSON属性名必须使用双引号
//3：else if只是方便我们写代码，实际上并不属于js的范畴
//4：运算符的优先级:加减的运算符(+,++,-)比等于(=)的优先级低;等于运算符(=)的优先级比&&运算符的优先级低;&&运算符的优先级大于||运算符的优先级
+,++，-，-- "<" = "<" || "<" &&
