/**
 * 基础知识
 * 1:代码(源码):特定的指令，用来指示特定的指令，指示计算机执行哪些任务
 * 2:语句：执行任务的一组单纯，数字和运算符的集合
 * 3:表达式:
 * 4:运算符:赋值(=);算术(+,-,*,/);复合赋值(+=,-=,*=,/=);递增，递减(++,--);对象属性访问(console.log());相等(==,===,!=,!==);比较(<,>,<=,>=);逻辑(&&,||)
 * 5:值与类型:值才有类型，变量没有类型;计算需要一个num;打印需要一个字符串,做出决策需要布尔值
 * 6:类型转换:
 * 7:代码注释:不可以没有注释；不可以过多注释（每一行）;代码应该解释为什么，而不是是说明
 * 8:变量->块->条件判断->循环->函数->作用域->
 */
{ //语句
  a = b * 2;
  //a,b:变量;2:字面值;=,*:运算符
}
